{
  "name": "@startup-blueprint/website",
  "version": "1.0.0",
  "private": true,
  "description": "Startup Blueprint landing page",
  "scripts": {
    "dev": "npx serve public -p 8080",
    "build": "echo 'Static site - no build needed'",
    "preview": "npx serve public -p 8080"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
