name: Format & Lint

on:
  workflow_call:
    outputs:
      final-commit-sha:
        description: 'Final commit SHA after formatting/linting'
        value: ${{ jobs.run.outputs.commit-sha }}

permissions:
  contents: write
  checks: write
  pull-requests: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      commit-sha: ${{ steps.output_sha.outputs.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Capture initial commit SHA
        id: initial_sha
        run: |
          INITIAL_SHA=$(git rev-parse HEAD)
          echo "sha=$INITIAL_SHA" >> $GITHUB_OUTPUT
          echo "📝 Initial commit SHA: $INITIAL_SHA"

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Check if lock file exists
        id: check_lockfile
        run: |
          if [ -f "pnpm-lock.yaml" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ pnpm-lock.yaml found"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ pnpm-lock.yaml not found - will generate it"
          fi

      - name: Setup Node.js (with cache)
        if: steps.check_lockfile.outputs.exists == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Setup Node.js (without cache)
        if: steps.check_lockfile.outputs.exists != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python (for potential Python projects)
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Setup Go (for future Golang projects)
        uses: actions/setup-go@v5
        with:
          go-version: 1.21
          cache: false

      - name: Install Node dependencies
        run: pnpm install

      - name: Install Python tools (if Python files exist)
        id: check_python
        run: |
          if find . -type f -name "*.py" \
             -not -path "./node_modules/*" \
             -not -path "./.git/*" \
             -not -path "./.venv/*" \
             -not -path "./venv/*" | grep -q .; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "🐍 Installing Python linting tools..."
            pip install --quiet black isort flake8
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⏭️ No Python files found, skipping Python tool installation"
          fi

      - name: Install SQL tools (if SQL files exist)
        id: check_sql
        run: |
          if find . -type f -name "*.sql" \
             -not -path "./node_modules/*" \
             -not -path "./.git/*" \
             -not -path "./build/*" \
             -not -path "./dist/*" | grep -q .; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "🗄️  Installing SQLFluff..."
            pip install --quiet sqlfluff
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⏭️ No SQL files found, skipping SQL tool installation"
          fi

      - name: Commit lock file if newly generated
        if: steps.check_lockfile.outputs.exists == 'false'
        run: |
          if [ -f "pnpm-lock.yaml" ]; then
            echo "📝 Lock file was generated, committing it..."

            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

            git add pnpm-lock.yaml
            git commit -m "chore: add pnpm-lock.yaml" -m "Auto-generated by CI workflow on first run." -m "This enables dependency caching for faster subsequent runs."

            git push

            echo "✅ Lock file committed successfully"
          else
            echo "⚠️ Lock file was not generated (unexpected)"
          fi

      - name: Install Go tools (for future)
        run: |
          if [ -d "apps/credibility-markets" ]; then
            go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
          else
            echo "⏭️ Skipping Go tools (no Go projects yet)"
          fi

      - name: Run Prettier (format all files)
        id: prettier
        continue-on-error: true
        run: |
          npx prettier --write \
            "**/*.{js,ts,jsx,tsx,json,md,yml,yaml,css,scss,html}" \
            --ignore-path .gitignore \
            --log-level warn 2>&1 | tee /tmp/prettier.log

          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -eq 0 ]; then
            echo "status=✅ Passed" >> $GITHUB_OUTPUT
            git diff --name-only > /tmp/prettier_changes.txt
          else
            echo "status=❌ Failed" >> $GITHUB_OUTPUT
            echo "errors=$(grep -c "Error" /tmp/prettier.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Run Black (format Python files)
        id: black
        continue-on-error: true
        run: |
          if [ "${{ steps.check_python.outputs.exists }}" == "true" ]; then
            echo "🐍 Running Black formatter..."

            find . -type f -name "*.py" \
              -not -path "./node_modules/*" \
              -not -path "./.git/*" \
              -not -path "./.venv/*" \
              -not -path "./venv/*" \
              -not -path "./build/*" \
              -not -path "./dist/*" \
              | xargs black --quiet 2>&1 | tee /tmp/black.log

            EXIT_CODE=${PIPESTATUS[0]}
            if [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              git diff --name-only > /tmp/black_changes.txt
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              echo "errors=$(grep -c "error" /tmp/black.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no Python files)" >> $GITHUB_OUTPUT
            echo "⏭️ No Python files found, skipping Black"
            touch /tmp/black_changes.txt
          fi

      - name: Run isort (sort Python imports)
        id: isort
        continue-on-error: true
        run: |
          if [ "${{ steps.check_python.outputs.exists }}" == "true" ]; then
            echo "🐍 Running isort..."

            find . -type f -name "*.py" \
              -not -path "./node_modules/*" \
              -not -path "./.git/*" \
              -not -path "./.venv/*" \
              -not -path "./venv/*" \
              -not -path "./build/*" \
              -not -path "./dist/*" \
              | xargs isort --quiet 2>&1 | tee /tmp/isort.log

            EXIT_CODE=${PIPESTATUS[0]}
            if [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              git diff --name-only > /tmp/isort_changes.txt
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              echo "errors=$(grep -c "ERROR" /tmp/isort.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no Python files)" >> $GITHUB_OUTPUT
            echo "⏭️ No Python files found, skipping isort"
            touch /tmp/isort_changes.txt
          fi

      - name: Run SQLFluff (format & lint SQL files)
        id: sqlfluff
        continue-on-error: true
        run: |
          if [ "${{ steps.check_sql.outputs.exists }}" == "true" ]; then
            echo "🗄️  Running SQLFluff formatter and linter..."

            # Use sqlite dialect for D1 databases
            sqlfluff fix --dialect sqlite --force . \
              --exclude-rules ambiguous.column_count 2>&1 | tee /tmp/sqlfluff.log

            if sqlfluff lint --dialect sqlite . \
              --exclude-rules ambiguous.column_count 2>&1 | tee -a /tmp/sqlfluff.log; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              git diff --name-only > /tmp/sqlfluff_changes.txt
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              # Count actual violations, not just lines
              echo "errors=$(grep -c "^L:" /tmp/sqlfluff.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              echo "warnings=$(grep -c "WARNING" /tmp/sqlfluff.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no SQL files)" >> $GITHUB_OUTPUT
            echo "⏭️ No SQL files found, skipping SQLFluff"
            touch /tmp/sqlfluff_changes.txt
          fi

      - name: Run ESLint (auto-fix JS/TS)
        id: eslint
        continue-on-error: true
        run: |
          if find . -type f \( -name "*.js" -o -name "*.ts" -o -name "*.jsx" -o -name "*.tsx" \) \
             -not -path "./node_modules/*" -not -path "./.git/*" | grep -q .; then

            echo "🔍 Found JS/TS files, running ESLint..."

            pnpm exec eslint \
              "**/*.{js,ts,jsx,tsx}" \
              --fix \
              --ignore-path .gitignore \
              --max-warnings 0 \
              --format json \
              --output-file /tmp/eslint.json \
              2>&1 | tee /tmp/eslint.log

            EXIT_CODE=${PIPESTATUS[0]}

            if [ $EXIT_CODE -eq 2 ] && grep -q "all of the files matching the glob pattern" /tmp/eslint.log; then
              echo "📝 All JS/TS files are config files (intentionally ignored)"
              echo "status=⏭️ Skipped (only config files)" >> $GITHUB_OUTPUT
              touch /tmp/eslint_changes.txt
            elif [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              git diff --name-only > /tmp/eslint_changes.txt
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              # Parse JSON output for detailed error counts
              if [ -f /tmp/eslint.json ]; then
                ERROR_COUNT=$(jq '[.[].errorCount] | add // 0' /tmp/eslint.json 2>/dev/null || echo 0)
                WARN_COUNT=$(jq '[.[].warningCount] | add // 0' /tmp/eslint.json 2>/dev/null || echo 0)
                echo "errors=$ERROR_COUNT" >> $GITHUB_OUTPUT
                echo "warnings=$WARN_COUNT" >> $GITHUB_OUTPUT
              else
                echo "errors=1" >> $GITHUB_OUTPUT
                echo "warnings=0" >> $GITHUB_OUTPUT
              fi
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no JS/TS files)" >> $GITHUB_OUTPUT
            echo "⏭️ No JavaScript/TypeScript files found, skipping ESLint"
            touch /tmp/eslint_changes.txt
          fi

      - name: Run stylelint (auto-fix CSS/SCSS)
        id: stylelint
        continue-on-error: true
        run: |
          if [ -f ".stylelintrc.json" ]; then
            pnpm exec stylelint \
              "**/*.{css,scss}" \
              --fix \
              --allow-empty-input 2>&1 | tee /tmp/stylelint.log

            EXIT_CODE=${PIPESTATUS[0]}
            if [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              git diff --name-only > /tmp/stylelint_changes.txt
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              echo "errors=$(grep -c "×" /tmp/stylelint.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no stylelint config)" >> $GITHUB_OUTPUT
            echo "⏭️ No .stylelintrc.json found, skipping stylelint"
            touch /tmp/stylelint_changes.txt
          fi

      - name: Run markdownlint (auto-fix Markdown)
        id: markdownlint
        continue-on-error: true
        run: |
          pnpm exec markdownlint-cli2 \
            "**/*.md" \
            "#node_modules" \
            "#.github" \
            --fix \
            --config .markdownlint.json 2>&1 | tee /tmp/markdownlint.log

          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -eq 0 ]; then
            echo "status=✅ Passed" >> $GITHUB_OUTPUT
            git diff --name-only > /tmp/markdownlint_changes.txt
          else
            echo "status=❌ Failed" >> $GITHUB_OUTPUT
            # Count errors from output
            echo "errors=$(grep -c "MD[0-9]\{3\}" /tmp/markdownlint.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Run yamllint (check YAML, no auto-fix)
        id: yamllint
        continue-on-error: true
        run: |
          if command -v yamllint &> /dev/null; then
            yamllint . 2>&1 | tee /tmp/yamllint.log
            EXIT_CODE=${PIPESTATUS[0]}
            if [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              echo "errors=$(grep -c "error" /tmp/yamllint.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              echo "warnings=$(grep -c "warning" /tmp/yamllint.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (yamllint not installed)" >> $GITHUB_OUTPUT
            echo "⏭️ yamllint not found, skipping YAML validation"
          fi

      - name: Run shellcheck (check bash scripts)
        id: shellcheck
        continue-on-error: true
        run: |
          if command -v shellcheck &> /dev/null; then
            if find . -type f -name "*.sh" -not -path "./node_modules/*" | grep -q .; then
              find . -type f -name "*.sh" -not -path "./node_modules/*" \
                -exec shellcheck --format=gcc {} + 2>&1 | tee /tmp/shellcheck.log
              EXIT_CODE=${PIPESTATUS[0]}
              if [ $EXIT_CODE -eq 0 ]; then
                echo "status=✅ Passed" >> $GITHUB_OUTPUT
              else
                echo "status=❌ Failed" >> $GITHUB_OUTPUT
                echo "errors=$(grep -c "error:" /tmp/shellcheck.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
                echo "warnings=$(grep -c "warning:" /tmp/shellcheck.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
                echo "info=$(grep -c "note:" /tmp/shellcheck.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
                exit 1
              fi
            else
              echo "status=⏭️ Skipped (no shell scripts)" >> $GITHUB_OUTPUT
              echo "⏭️ No .sh files found, skipping shellcheck"
            fi
          else
            echo "status=⏭️ Skipped (shellcheck not installed)" >> $GITHUB_OUTPUT
            echo "⏭️ shellcheck not found, skipping shell script validation"
          fi

      - name: Run flake8 (lint Python files)
        id: flake8
        continue-on-error: true
        run: |
          if [ "${{ steps.check_python.outputs.exists }}" == "true" ]; then
            echo "🐍 Running flake8 linter..."

            find . -type f -name "*.py" \
              -not -path "./node_modules/*" \
              -not -path "./.git/*" \
              -not -path "./.venv/*" \
              -not -path "./venv/*" \
              -not -path "./build/*" \
              -not -path "./dist/*" \
              | xargs flake8 --max-line-length=88 --extend-ignore=E203,W503 \
              2>&1 | tee /tmp/flake8_output.txt

            EXIT_CODE=${PIPESTATUS[0]}

            if [ $EXIT_CODE -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
              echo "errors=0" >> $GITHUB_OUTPUT
            else
              ERROR_COUNT=$(grep -c "^\./" /tmp/flake8_output.txt 2>/dev/null || echo "0")
              echo "status=❌ Failed ($ERROR_COUNT violations)" >> $GITHUB_OUTPUT
              echo "errors=$ERROR_COUNT" >> $GITHUB_OUTPUT

              echo "::error::flake8 found $ERROR_COUNT violations"
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no Python files)" >> $GITHUB_OUTPUT
            echo "errors=0" >> $GITHUB_OUTPUT
            echo "⏭️ No Python files found, skipping flake8"
            touch /tmp/flake8_output.txt
          fi

      - name: Run gofmt (format Go files)
        id: gofmt
        continue-on-error: true
        run: |
          if [ -d "apps/credibility-markets" ]; then
            gofmt -w apps/credibility-markets/
            echo "status=✅ Passed" >> $GITHUB_OUTPUT
            git diff --name-only > /tmp/gofmt_changes.txt
          else
            echo "status=⏭️ Skipped (no Go projects)" >> $GITHUB_OUTPUT
            echo "⏭️ No Go projects found, skipping gofmt"
            touch /tmp/gofmt_changes.txt
          fi

      - name: Run golangci-lint (lint Go files)
        id: golangci
        continue-on-error: true
        run: |
          if [ -d "apps/credibility-markets" ]; then
            if command -v golangci-lint &> /dev/null; then
              cd apps/credibility-markets
              golangci-lint run --fix 2>&1 | tee /tmp/golangci.log
              EXIT_CODE=${PIPESTATUS[0]}
              if [ $EXIT_CODE -eq 0 ]; then
                echo "status=✅ Passed" >> $GITHUB_OUTPUT
                git diff --name-only > /tmp/golangci_changes.txt
              else
                echo "status=❌ Failed" >> $GITHUB_OUTPUT
                echo "errors=$(grep -c "^Error:" /tmp/golangci.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
                exit 1
              fi
            else
              echo "status=⏭️ Skipped (golangci-lint not installed)" >> $GITHUB_OUTPUT
              echo "⏭️ golangci-lint not found, skipping Go linting"
              touch /tmp/golangci_changes.txt
            fi
          else
            echo "status=⏭️ Skipped (no Go projects)" >> $GITHUB_OUTPUT
            echo "⏭️ No Go projects found, skipping golangci-lint"
            touch /tmp/golangci_changes.txt
          fi

      - name: Lint commit messages
        id: commitlint
        continue-on-error: true
        run: |
          cat > commitlint.config.js << 'EOF'
          module.exports = {
            extends: ['@commitlint/config-conventional'],
            rules: {
              'type-enum': [
                2,
                'always',
                [
                  'feat',
                  'fix',
                  'docs',
                  'style',
                  'refactor',
                  'perf',
                  'test',
                  'chore',
                  'ci',
                  'build',
                  'revert',
                ],
              ],
              'scope-case': [2, 'always', 'kebab-case'],
              'subject-case': [0],
              'subject-empty': [2, 'never'],
              'subject-full-stop': [2, 'never', '.'],
              'header-max-length': [2, 'always', 100],
              'body-leading-blank': [2, 'always'],
              'body-max-line-length': [0],
              'footer-leading-blank': [2, 'always'],
            },
          };
          EOF

          BASE_REF="${{ github.base_ref || 'main' }}"
          echo "🔍 Fetching base branch: origin/$BASE_REF"
          git fetch origin "$BASE_REF" || echo "⚠️ Could not fetch $BASE_REF, using HEAD~1"

          if git rev-parse "origin/$BASE_REF" >/dev/null 2>&1; then
            BASE_SHA=$(git merge-base "origin/$BASE_REF" HEAD)
            echo "🔍 Linting commits from $BASE_SHA (origin/$BASE_REF) to HEAD"
            pnpm exec commitlint --from "$BASE_SHA" --to HEAD --verbose 2>&1 | tee /tmp/commitlint.log
          else
            echo "🔍 Linting last commit only (no base branch found)"
            pnpm exec commitlint --from HEAD~1 --to HEAD --verbose 2>&1 | tee /tmp/commitlint.log
          fi

          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -eq 0 ]; then
            echo "status=✅ Passed" >> $GITHUB_OUTPUT
          else
            echo "status=❌ Failed" >> $GITHUB_OUTPUT
            echo "errors=$(grep -c "❌" /tmp/commitlint.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Check TypeScript types
        id: typescript
        continue-on-error: true
        run: |
          if [ -f "tsconfig.json" ] || [ -f "apps/*/tsconfig.json" ]; then
            echo "🔍 Checking TypeScript types..."

            HAS_ERRORS=0

            if [ -f "tsconfig.json" ]; then
              pnpm exec tsc --noEmit --skipLibCheck 2>&1 | tee /tmp/typescript.log
              if [ ${PIPESTATUS[0]} -ne 0 ]; then
                HAS_ERRORS=1
              fi
            fi

            for tsconfig in apps/*/tsconfig.json; do
              if [ -f "$tsconfig" ]; then
                echo "Checking $tsconfig"
                cd $(dirname $tsconfig)
                pnpm exec tsc --noEmit --skipLibCheck 2>&1 | tee -a /tmp/typescript.log
                if [ ${PIPESTATUS[0]} -ne 0 ]; then
                  HAS_ERRORS=1
                fi
                cd - > /dev/null
              fi
            done

            if [ $HAS_ERRORS -eq 0 ]; then
              echo "status=✅ Passed" >> $GITHUB_OUTPUT
            else
              echo "status=❌ Failed" >> $GITHUB_OUTPUT
              echo "errors=$(grep -c "error TS" /tmp/typescript.log 2>/dev/null || echo 0)" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "status=⏭️ Skipped (no TypeScript config)" >> $GITHUB_OUTPUT
            echo "⏭️ No tsconfig.json found, skipping TypeScript type checking"
          fi

      - name: Unstage workflow files (security safeguard)
        if: always()
        run: |
          if [ -d ".github/workflows" ]; then
            echo "🔒 Ensuring workflow files are not staged..."
            git reset HEAD .github/workflows/ 2>/dev/null || true
            git checkout -- .github/workflows/ 2>/dev/null || true
            echo "✅ Workflow files excluded from commit"
          fi

      - name: Clean tracking files (remove unstaged workflow files)
        if: always()
        run: |
          echo "🧹 Cleaning tracking files to match staged files..."

          for tracking_file in /tmp/*_changes.txt; do
            if [ -f "$tracking_file" ]; then
              grep -v "^\.github/workflows/" "$tracking_file" > "${tracking_file}.tmp" 2>/dev/null || touch "${tracking_file}.tmp"
              mv "${tracking_file}.tmp" "$tracking_file"

              REMAINING=$(wc -l < "$tracking_file" 2>/dev/null || echo 0)
              if [ "$REMAINING" -gt 0 ]; then
                echo "✅ $(basename $tracking_file): $REMAINING file(s) remaining after cleanup"
              fi
            fi
          done

          echo "✅ Tracking files cleaned - commit message will be accurate"

      - name: Check for changes
        id: check_changes
        if: always()
        run: |
          git add -A

          if git diff --cached --name-only | grep -q "^\.github/workflows/"; then
            echo "::error::Workflow files are staged despite safeguards!"
            echo "::error::This should never happen. Aborting."
            git diff --cached --name-only | grep "^\.github/workflows/"
            exit 1
          fi

          if git diff --cached --quiet; then
            echo "changes=false" >> $GITHUB_OUTPUT
            echo "file_count=0" >> $GITHUB_OUTPUT
            echo "✅ No formatting/linting changes needed"
          else
            echo "changes=true" >> $GITHUB_OUTPUT
            FILE_COUNT=$(git diff --cached --name-only | wc -l)
            echo "file_count=$FILE_COUNT" >> $GITHUB_OUTPUT
            echo "📝 Changes detected in $FILE_COUNT file(s)"

            git diff --cached --name-only > /tmp/changed_files.txt
          fi

      - name: Generate enhanced summary report
        if: always()
        run: |
          FAILED=false
          if [[ "${{ steps.prettier.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.black.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.isort.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.sqlfluff.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.eslint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.stylelint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.markdownlint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.yamllint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.shellcheck.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.flake8.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.gofmt.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.golangci.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.commitlint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.typescript.conclusion }}" == "failure" ]]; then
            FAILED=true
          fi

          if [ "$FAILED" == "true" ]; then
            echo "## ❌ Format & Lint Summary: Failures Detected" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **One or more checks failed. See details below and check " \
              "the [full log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) " \
              "for errors.**" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ steps.check_changes.outputs.changes }}" == "true" ]; then
            echo "## 📝 Format & Lint Summary: Changes Applied" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ✅ Format & Lint Summary: All Clean!" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🪧 Tools Run" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tool | Status | Errors | Warnings | Info | Sample Issues |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|--------|----------|------|---------------|" >> $GITHUB_STEP_SUMMARY

          # Function to get first N lines from a log file
          get_sample_issues() {
            local log_file="$1"
            local pattern="$2"
            local max_lines="${3:-3}"

            if [ -f "$log_file" ]; then
              grep "$pattern" "$log_file" 2>/dev/null | head -n $max_lines | sed 's/^/`/;s/$/`/' | tr '\n' ' ' | sed 's/ $//' || echo "-"
            else
              echo "-"
            fi
          }

          # Prettier
          PRETTIER_ISSUES=$(get_sample_issues "/tmp/prettier.log" "Error")
          echo "| **Prettier** | ${{ steps.prettier.outputs.status || '❌ Failed' }} | ${{ steps.prettier.outputs.errors || '0' }} | - | - | $PRETTIER_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # Black
          BLACK_ISSUES=$(get_sample_issues "/tmp/black.log" "error")
          echo "| **Black** | ${{ steps.black.outputs.status || '❌ Failed' }} | ${{ steps.black.outputs.errors || '0' }} | - | - | $BLACK_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # isort
          ISORT_ISSUES=$(get_sample_issues "/tmp/isort.log" "ERROR")
          echo "| **isort** | ${{ steps.isort.outputs.status || '❌ Failed' }} | ${{ steps.isort.outputs.errors || '0' }} | - | - | $ISORT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # SQLFluff - Only show issues if it failed
          if [ "${{ steps.sqlfluff.conclusion }}" == "failure" ]; then
            SQLFLUFF_ISSUES=$(get_sample_issues "/tmp/sqlfluff.log" "^L:")
          else
            SQLFLUFF_ISSUES="-"
          fi
          echo "| **SQLFluff** | ${{ steps.sqlfluff.outputs.status || '❌ Failed' }} | ${{ steps.sqlfluff.outputs.errors || '0' }} | ${{ steps.sqlfluff.outputs.warnings || '0' }} | - | $SQLFLUFF_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # ESLint - Include both errors and warnings
          if [ -f "/tmp/eslint.json" ]; then
            ESLINT_ISSUES=$(jq -r '[.[].messages[] | select(.severity == 1 or .severity == 2) | "\(.ruleId) (\(.line):\(.column))"] | .[0:3] | join(", ")' /tmp/eslint.json 2>/dev/null || echo "-")
          else
            ESLINT_ISSUES="-"
          fi
          echo "| **ESLint** | ${{ steps.eslint.outputs.status || '❌ Failed' }} | ${{ steps.eslint.outputs.errors || '0' }} | ${{ steps.eslint.outputs.warnings || '0' }} | - | $ESLINT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # stylelint
          STYLELINT_ISSUES=$(get_sample_issues "/tmp/stylelint.log" "×")
          echo "| **stylelint** | ${{ steps.stylelint.outputs.status || '❌ Failed' }} | ${{ steps.stylelint.outputs.errors || '0' }} | - | - | $STYLELINT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # markdownlint
          MDLINT_ISSUES=$(get_sample_issues "/tmp/markdownlint.log" "MD[0-9]")
          echo "| **markdownlint** | ${{ steps.markdownlint.outputs.status || '❌ Failed' }} | ${{ steps.markdownlint.outputs.errors || '0' }} | - | - | $MDLINT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # yamllint
          YAMLLINT_ISSUES=$(get_sample_issues "/tmp/yamllint.log" "error")
          echo "| **yamllint** | ${{ steps.yamllint.outputs.status || '❌ Failed' }} | ${{ steps.yamllint.outputs.errors || '0' }} | ${{ steps.yamllint.outputs.warnings || '0' }} | - | $YAMLLINT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # shellcheck
          SHELLCHECK_ISSUES=$(get_sample_issues "/tmp/shellcheck.log" "error:")
          echo "| **shellcheck** | ${{ steps.shellcheck.outputs.status || '❌ Failed' }} | ${{ steps.shellcheck.outputs.errors || '0' }} | ${{ steps.shellcheck.outputs.warnings || '0' }} | ${{ steps.shellcheck.outputs.info || '0' }} | $SHELLCHECK_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # flake8
          if [ -f "/tmp/flake8_output.txt" ] && [ "${{ steps.flake8.outputs.errors }}" != "0" ]; then
            FLAKE8_ISSUES=$(head -n 3 /tmp/flake8_output.txt | sed 's/^/`/;s/$/`/' | tr '\n' ' ' | sed 's/ $//' || echo "-")
          else
            FLAKE8_ISSUES="-"
          fi
          echo "| **flake8** | ${{ steps.flake8.outputs.status || '❌ Failed' }} | ${{ steps.flake8.outputs.errors || '0' }} | - | - | $FLAKE8_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # gofmt
          echo "| **gofmt** | ${{ steps.gofmt.outputs.status || '❌ Failed' }} | 0 | - | - | - |" >> $GITHUB_STEP_SUMMARY

          # golangci-lint
          GOLANGCI_ISSUES=$(get_sample_issues "/tmp/golangci.log" "^Error:")
          echo "| **golangci-lint** | ${{ steps.golangci.outputs.status || '❌ Failed' }} | ${{ steps.golangci.outputs.errors || '0' }} | - | - | $GOLANGCI_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # commitlint
          COMMITLINT_ISSUES=$(get_sample_issues "/tmp/commitlint.log" "❌")
          echo "| **commitlint** | ${{ steps.commitlint.outputs.status || '❌ Failed' }} | ${{ steps.commitlint.outputs.errors || '0' }} | - | - | $COMMITLINT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          # TypeScript
          TYPESCRIPT_ISSUES=$(get_sample_issues "/tmp/typescript.log" "error TS")
          echo "| **TypeScript** | ${{ steps.typescript.outputs.status || '❌ Failed' }} | ${{ steps.typescript.outputs.errors || '0' }} | - | - | $TYPESCRIPT_ISSUES |" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$FAILED" == "true" ]; then
            echo "### 🛠️ Action Required" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Please review the failed checks above and fix the issues:" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "1. Check the [full action log](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for detailed error messages" >> $GITHUB_STEP_SUMMARY
            echo "2. Fix the issues locally" >> $GITHUB_STEP_SUMMARY
            echo "3. Push your changes" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ steps.check_changes.outputs.changes }}" == "true" ]; then
            echo "### 📝 Changed Files (${{ steps.check_changes.outputs.file_count }})" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat /tmp/changed_files.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **Auto-commit will be created below**" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🎉 No Changes Needed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "All code is properly formatted and linted!" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Fail job if any check failed
        if: always()
        run: |
          if [[ "${{ steps.prettier.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.black.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.isort.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.sqlfluff.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.eslint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.stylelint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.markdownlint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.yamllint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.shellcheck.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.flake8.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.gofmt.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.golangci.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.commitlint.conclusion }}" == "failure" ]] || \
             [[ "${{ steps.typescript.conclusion }}" == "failure" ]]; then
            echo "❌ One or more format/lint checks failed"
            exit 1
          fi

      - name: Build commit message with per-tool breakdown
        if: steps.check_changes.outputs.changes == 'true'
        id: build_message
        run: |
          cat > /tmp/commit_message.txt << 'COMMIT_MSG_EOF'
          chore: auto-format and lint fixes

          Applied automatic formatting and linting fixes.

          📝 Changed Files (${{ steps.check_changes.outputs.file_count }})
          COMMIT_MSG_EOF

          for tool_name in "Prettier" "Black" "isort" "SQLFluff" "ESLint" "stylelint" "markdownlint" "gofmt" "golangci-lint"; do
            case "$tool_name" in
              "Prettier") tool_file="/tmp/prettier_changes.txt" ;;
              "Black") tool_file="/tmp/black_changes.txt" ;;
              "isort") tool_file="/tmp/isort_changes.txt" ;;
              "SQLFluff") tool_file="/tmp/sqlfluff_changes.txt" ;;
              "ESLint") tool_file="/tmp/eslint_changes.txt" ;;
              "stylelint") tool_file="/tmp/stylelint_changes.txt" ;;
              "markdownlint") tool_file="/tmp/markdownlint_changes.txt" ;;
              "gofmt") tool_file="/tmp/gofmt_changes.txt" ;;
              "golangci-lint") tool_file="/tmp/golangci_changes.txt" ;;
            esac

            if [ -f "$tool_file" ] && [ -s "$tool_file" ]; then
              echo "" >> /tmp/commit_message.txt
              echo "$tool_name:" >> /tmp/commit_message.txt
              while IFS= read -r file; do
                echo "- $file" >> /tmp/commit_message.txt
              done < "$tool_file"
            fi
          done

      - name: Commit and push changes
        if: steps.check_changes.outputs.changes == 'true'
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git commit -F /tmp/commit_message.txt

          BOT_COMMIT_SHA=$(git rev-parse HEAD)
          echo "sha=$BOT_COMMIT_SHA" >> $GITHUB_OUTPUT

          git push

          echo "✅ Committed and pushed formatting fixes"
          echo "📝 Bot commit SHA: $BOT_COMMIT_SHA"

      - name: Output final commit SHA
        id: output_sha
        if: always()
        run: |
          if [ "${{ steps.check_changes.outputs.changes }}" == "true" ]; then
            FINAL_SHA="${{ steps.commit.outputs.sha }}"
            echo "🤖 Using bot commit SHA: $FINAL_SHA"
          else
            FINAL_SHA="${{ steps.initial_sha.outputs.sha }}"
            echo "📝 Using original commit SHA: $FINAL_SHA"
          fi

          echo "sha=$FINAL_SHA" >> $GITHUB_OUTPUT

      - name: Update summary with final commit
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check_changes.outputs.changes }}" == "true" ]; then
            echo "### ✅ Auto-Commit Created" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Commit SHA:** \`${{ steps.commit.outputs.sha }}\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🎉 No Changes Required" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Original Commit SHA:** \`${{ steps.initial_sha.outputs.sha }}\`" >> $GITHUB_STEP_SUMMARY
          fi
